{
  "pipeline": {
    "training": {
      "checkpoints_dir": "checkpoints",
      "loss": {
        "class": "CrossEntropyLoss",
        "kwargs": {
          "reduction": "mean"
        },
        "inputs": ["y_hat", "y"],
        "transform": "examples.ocr.transforms.transform"
      },
      "metrics": {
        "loss": {
          "inputs": ["y_hat", "y"],
          "transform": "examples.ocr.transforms.transform"
        }
      }
    }
  }
}
